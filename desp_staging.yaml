# DESP staging environment overrides.
# Keys here are deep-merged on top of common.yaml by the signer service.
# Use this to hide diagnostics, override descriptions, etc. for the DESP staging dashboard.

